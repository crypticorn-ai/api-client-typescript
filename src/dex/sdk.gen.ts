
import { createClient as createNativeClient, createConfig, type OptionsLegacyParser } from '@hey-api/client-fetch';

import type { GetSignalsData, GetSignalsError, GetSignalsResponse, GetTopSignalsData, GetTopSignalsError, GetTopSignalsResponse, GetSignalStatsError, GetSignalStatsResponse, GetTokenIntelData, GetTokenIntelError, GetTokenIntelResponse, GetSignalDetailData, GetSignalDetailError, GetSignalDetailResponse, AnalyzeSignalData, AnalyzeSignalError, AnalyzeSignalResponse, PingError, PingResponse, GetMetricsError, GetMetricsResponse } from './types.gen';

export function createClient(
  baseUrl: string,
  headers: any,
  fetch = globalThis.fetch,
) {
  const client = createNativeClient(
    createConfig({
      baseUrl,
      fetch,
      headers,
    })
  );

  // This file is auto-generated by @hey-api/openapi-ts





/**
 * Get Signals
 * Retrieve all trading signals with pagination, filtering, and sorting support.
 *
 * Returns signals enriched with token metadata. Default sort is by call time (newest first).
 * If the user is not authenticated, signals from the last 24 hours are excluded from the results.
 */
const getSignals = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetSignalsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSignalsResponse, GetSignalsError, ThrowOnError>({
        ...options,
        url: '/signals'
    });
};

/**
 * Get Top Signals
 * Retrieve top-performing signals ranked by performance within a specified time window.
 *
 * Returns the highest-performing signals from the last X hours (default: 24 hours).
 */
const getTopSignals = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetTopSignalsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTopSignalsResponse, GetTopSignalsError, ThrowOnError>({
        ...options,
        url: '/signals/top'
    });
};

/**
 * Get Stats
 * Retrieve comprehensive statistics and analytics for all trading signals. Returns hourly-updated aggregated metrics.
 */
const getSignalStats = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSignalStatsResponse, GetSignalStatsError, ThrowOnError>({
        ...options,
        url: '/signals/stats'
    });
};

/**
 * Get Token Intel
 * Return the latest token intelligence snapshot for an address.
 *
 * Unlike the detail endpoint this does NOT require the token to exist in
 * the signals table — any address that has been ingested via the CLI or
 * worker will be served.  If intel is stale or missing a background fetch
 * is automatically triggered (same as the detail endpoint).
 */
const getTokenIntel = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTokenIntelData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTokenIntelResponse, GetTokenIntelError, ThrowOnError>({
        ...options,
        url: '/signals/intel/{address}'
    });
};

/**
 * Get Signal Detail
 * Get detailed signal information including token intelligence.
 *
 * If intel data is missing or older than 10 minutes a background fetch is
 * automatically triggered.  The response includes ``intel_stale: true`` so
 * the caller knows to poll for fresh data.
 */
const getSignalDetail = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetSignalDetailData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSignalDetailResponse, GetSignalDetailError, ThrowOnError>({
        ...options,
        url: '/signals/{address}/detail'
    });
};

/**
 * Analyze Signal
 * Fetch fresh intelligence, save snapshot, return results.
 *
 * Uses fast fetch inline for immediate response, then kicks off a full
 * browser fetch in the background. Rate-limited per user.
 */
const analyzeSignal = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AnalyzeSignalData, ThrowOnError>) => {
    return (options?.client ?? client).post<AnalyzeSignalResponse, AnalyzeSignalError, ThrowOnError>({
        ...options,
        url: '/signals/{address}/analyze'
    });
};

/**
 * Ping
 * Returns 'OK' if the API is running.
 */
const ping = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<PingResponse, PingError, ThrowOnError>({
        ...options,
        url: '/'
    });
};

/**
 * Metrics
 * Get Prometheus metrics for the application. Returns plain text.
 */
const getMetrics = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<GetMetricsResponse, GetMetricsError, ThrowOnError>({
        ...options,
        url: '/metrics'
    });
};

  return {
    getSignals,
    getTopSignals,
    getSignalStats,
    getTokenIntel,
    getSignalDetail,
    analyzeSignal,
    ping,
    getMetrics,
  };
}
